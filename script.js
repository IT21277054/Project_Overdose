      /*
      let country = "Sri Lanka";
      let continent = "Asia";
      let population = "22 million";

      console.log(country);
      console.log(continent);
      console.log(population);
      console.log(typeof country);
      

      let tempValue = 'Omce ! ';
      console.log(tempValue);
      console.log(typeof tempValue);

      tempValue = 'RIP ! ';

      console.log(tempValue);
      console.log(typeof tempValue);

      let emptyValue ;

      console.log(emptyValue);
      console.log(typeof emptyValue);

      emptyValue = 2021;

      console.log(emptyValue);
      console.log(typeof emptyValue);
      

      let language;
      const country = "Sri Lanka";
      const continent = "Asia";
      const population = 23;
      let isIsland = true;
      const halfCountry = population / 2;

      console.log(typeof language);
      console.log(typeof country);
      console.log(typeof continent);
      console.log(typeof population);
      console.log(typeof isIsland);
      console.log(halfCountry.toFixed(2));

      let finlandPopulation = 6 ;
      let isMorePopulation = population > finlandPopulation ;
      console.log(isMorePopulation);

      let averagePopulation = 33 ; 
      const isHigherPopulation = population > averagePopulation ;
      console.log(isHigherPopulation);

      const description = `Portugal is in Europe and its 11 million people speak portuguese`;
      console.log(description);

      const country = `Portugal`;
      const portugalPopulation = 11 ;
      const portugalLanguage = `portuguese `;

      const templateDescription = `${country} is in Europe and its ${portugalPopulation} million people speak ${portugalLanguage} `;
      console.log(templateDescription);



      
           function BMI( height ,  weight){
                
                let bmi = weight / (height ** 2) ;
                return bmi;
           }
           const weightMark = 95;
           const weightJohn = 85;

           const heightMark = 1.88;
           const heightJohn = 1.76;

           let markBMI = BMI(heightMark , weightMark ) ;
           let johnBMI = BMI(heightJohn , weightJohn);

           const markHigherBMI = markBMI > johnBMI;

           console.log(markBMI.toFixed(2) );
           console.log(johnBMI.toFixed(2)) ;
           console.log(markHigherBMI);

           if(markHigherBMI){
                console.log(`Marks BMI (${markBMI.toFixed(2)}) is higher than john's BMI (${johnBMI.toFixed(2)} `);
           }
           else{
                console.log(`Johns BMI (${markBMI.toFixed(2)}) is higher than mark's BMI (${johnBMI.toFixed(2)}) `);
           }
      
      
           const firstName = 'Sudheera';   
           const job = "genius";
           const birthYear = 2001;
           const currentYear = 2021;

           let sudheera = "I'm " + firstName + " , a " + (currentYear - birthYear) + "years old " + job ;
           
           let tempValue = `I'm ${firstName}, a ${currentYear - birthYear} years old ${job} ! `

           console.log(sudheera);
           console.log(tempValue);

           console.log("Hoda nthi \n\
           Dushta \n\
           shamry");
      
      
           let age = 20 ;

           const isOldEnough = age >= 18 ;

           if (isOldEnough) {
                console.log(`Sudheera can start driving license 🚗`);
           }

      

      const country = `Sri Lanka`;
      const population = 22 ;
      const averagePopulation = 33 ;

      let highThanAverage = population > averagePopulation ;

      if (highThanAverage){
           console.log(`${country} population is above than average population of a country `);

      }
      else {
           console.log(`${country} population is ${averagePopulation - population } million below than average population of a country`);
      }

      

      const inputYear = `1991`;
      console.log(inputYear);
      console.log(Number(inputYear) , inputYear);

      console.log(String(23), 23);

      console.log('I am ' + 20 + ' years old !');
      console.log(`I am 23 years old`);

      console.log('23' + 10 );
      console.log('23' - 3 );

      console.log('23' * 2);

      let n = '1' + 1 ;
      n = n -1 ;
      console.log(n);
      

      console.log(Boolean(0));
      console.log(Boolean(undefined));
      console.log(Boolean(' '));
      console.log(Boolean(null));
      console.log(Boolean({}));
      
      const money = 0;

      if (money) {
           console.log(`Don't spend it all `);
      }
      else{
           console.log(`You should get a job`);
      }

     

      const favorite = Number(prompt(`Enter number`));

      if(favorite === 69 ) {console.log(`Okay Cool ! `);

     }
     else if(favorite === 20 ) console.log(`Okay` );

     else console.log(`kk`);
     
    
     let teamDolphinsTotal = 0 ;
     let teamKoalasTotal = 0;

     for (let i = 0; i < 3 ; i++) {
     const teamDolphins = Number(prompt(`Enter team dolphin score ${i + 1 } ` ));
     teamDolphinsTotal+= teamDolphins;

     const teamKoalas = Number(prompt(`Enter team koalas score ${i + 1 } ` ));
     teamKoalasTotal+= teamKoalas;
     }

     const averageTeamDolphins = teamDolphinsTotal / 3 ;
     const averageTeamKoalas = teamKoalasTotal / 3 ;

     if (averageTeamDolphins > averageTeamKoalas) console.log(`Team dolphins has higher average score` );
     else console.log(`Team koalas has higher average score`);

     if (teamDolphinsTotal > 100 && teamDolphinsTotal > teamKoalasTotal ) console.log(`Team dolphins has won the tournament `);
     else if (teamDolphinsTotal === teamKoalasTotal && teamKoalasTotal > 100 && teamDolphinsTotal > 100 ) console.log(`Both team has same score match is draw`);
     else if (teamKoalasTotal > 100 && teamKoalasTotal > teamDolphinsTotal) console.log(`Team koalas has won the tournament`);
     else console.log(`Both team score less than 100  so there is no winner in the match `);
*/
     const day = `monday` ;
     




